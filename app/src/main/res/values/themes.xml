<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.StrangerThings" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/stranger_red</item>
        <item name="colorPrimaryVariant">@color/stranger_black</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/stranger_red</item>
        <item name="colorSecondaryVariant">@color/stranger_black</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="fontText">
        <item name="android:fontFamily">serif-monospace</item>
    </style>

    <style name="redText" parent="fontText">
        <item name="android:textColor">@color/stranger_red</item>
        <item name="android:backgroundTint">@color/stranger_red</item>
        <item name="android:textAlignment">center</item>
        <item name="android:textAllCaps">true</item>
    </style>

    <style name="blackText" parent="fontText">
        <item name="android:textColor">@color/stranger_black</item>
        <item name="android:textAlignment">center</item>
        <item name="android:textAllCaps">true</item>
    </style>

    <style name="editTextCharacterName" parent="redText">
        <item name="android:textColorHint">@color/stranger_red</item>
        <item name="android:width">295dp</item>
        <item name="android:textAllCaps">true</item>
    </style>

    <style name="buttonSearch" parent="blackText">
        <item name="backgroundColor">@color/stranger_red</item>
        <item name="android:width">290dp</item>
    </style>

    <style name="ThemeOverlay.StrangerThings.AppWidgetContainer" parent="">
        <item name="appWidgetBackgroundColor">@color/light_blue_600</item>
        <item name="appWidgetTextColor">@color/light_blue_50</item>
    </style>

</resources>